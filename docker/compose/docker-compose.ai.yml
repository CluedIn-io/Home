services:     
  ai-host:
    image: ${CLUEDIN_CONTAINER_REGISTRY:-cluedinprod.azurecr.io}/cluedin/ai-host:4.5.0-alpha.1
    ports:
    - 11434:11434
    environment:
      - OLLAMA_NUM_PARALLEL=4
#    deploy:
#      resources:
#        reservations:
#          devices:
#            - driver: nvidia
#              count: all
#              capabilities: [gpu]